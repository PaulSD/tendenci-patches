diff --git a/tendenci/apps/memberships/views.py b/tendenci/apps/memberships/views.py
index aa7ab6253..371ef676a 100644
--- a/tendenci/apps/memberships/views.py
+++ b/tendenci/apps/memberships/views.py
@@ -1,3 +1,5 @@
+import logging
+_log = logging.getLogger(__name__)
 import os
 import math
 from decimal import Decimal
@@ -1294,6 +1296,8 @@ def membership_default_add(request, slug='', membership_id=None,
                 args=[memberships[0].guid]
             ))
 
+    if request.method == 'POST':
+        _log.info('%s %s %s %s %s'%(user_form.errors, profile_form.errors, demographics_form.errors, membership_form.errors, captcha_form.errors))
     context = {
         'app': app,
         'app_fields': app_fields,
@@ -1429,6 +1433,8 @@ def membership_default_edit(request, id, template='memberships/applications/add.
             messages.success(request, _('Successfully updated Membership Information.'))
             return redirect(reverse('membership.details', kwargs={'id': membership.id}))
 
+    if request.method == 'POST':
+        _log.info('%s %s %s %s'%(user_form.errors, profile_form.errors, demographics_form.errors, membership_form2.errors))
     context = {
         'app': app,
         'app_fields': app_fields,
